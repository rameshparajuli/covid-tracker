/**
 * @format
 */
import {AppRegistry} from 'react-native';
import App from './App';
import Covid from './src/Covid';
import {name as appName} from './app.json';

AppRegistry.registerComponent(appName, () => Covid);
